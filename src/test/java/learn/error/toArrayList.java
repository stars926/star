package learn.error;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

/**
 * @author zt
 * @date 2022/12/16 14:19
 */
public class toArrayList {

	// 不可变和可变
//	List<String> strings1 = Arrays.asList(source.split(","));
//	List<String> strings = Arrays.stream(source.split(",")).collect(Collectors.toList());
}
